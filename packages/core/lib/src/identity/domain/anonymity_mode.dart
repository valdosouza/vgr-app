/// How a user's identity is exposed to other users on a Report
/// (decision 6). `identifiedWithReward` requires a completed
/// registration (decision 4).
enum AnonymityMode { anonymous, identifiedNoReward, identifiedWithReward }
